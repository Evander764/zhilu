# 先选题再检索：线上发布

2026-09-08，[线上入口](https://icnimtns216g.feishuapp.com/app/app_17dg665m8tq)。

- 发布 ID：7683077447163694276，状态 finished。
- 线上运行提交：338874b00ee3fd3ee2dc789b1340baa61a131b75。
- 六个不同方向的选题作为首页入口，不带预置回答和展开动作；选择后才调用服务端 CLI 搜索，阅读从选中的真实结果开始。
- 官方 zhihu-cli 0.5.0 随服务端包发布。构建检查官方下载来源、大小、SHA-256、唯一归档成员、ELF 架构，以及 version 子命令返回的精确版本。
- 沿用当前公开应用和既有服务端凭据，没有更改访问范围或精选内容库。

## 线上证据

匿名首页 HTTP 200，静态资源路径包含本次运行提交；匿名 status HTTP 200，searchEnabled=true。验证请求没有账号 Cookie 或 Authorization，只使用同值的 CSRF Cookie/header。

匿名选题搜索“人为什么会觉得年纪越大，时间过得越快？”HTTP 200，返回 5 个真实问题，cached=false。读取选中的 1998112125915267964 返回相同 ID 及 3 条来源。线上日志在 2026-09-08 16:29:58（上海时区）记录 ZhihuCli 的 search_completed，确认是托管实例运行 CLI。

证据保存在仓库外 deliverables/zhilu-topic-first-online/ 的 release-final.json、anonymous.json、search.json。55 项单元测试、lint 和前后端构建已通过。

浏览器线上点击复验未完成：ego-browser 返回任务空间已由用户接管并结束，按接管边界停止，没有另开空间或切换浏览器规避。发布前的页面结构和交互测试已有记录，但不能代替本次线上浏览器复验。用户允许继续同一浏览器任务空间后，再检查首页零请求、选题点击、结果阅读和桌面/手机布局。

## 首次发布失败与恢复

首次发布 7683076627589827882 在 CLI 版本检查中失败，原因是使用了不支持的 --version；旧线上版本保持可用。改用官方 version 子命令并实际核验后，第二次发布成功。版本检查保留在构建入口，防止缺失或不兼容 CLI 被直接发布。

回退：撤销本轮代码至此前已验证的 f4cd793，再推送并创建发布；不要删除资料库或更改访问权限。
