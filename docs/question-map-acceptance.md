# 问题图谱验收 · 2026-09-07

## 已完成的本地与数据检查

- 内容：8 个真实知乎问题、12 条来源、11 条精选连接、3 条示范路径。12 条短摘录与作者逐项在原页面核对；一条无障碍快照截断的段落另用页面可见文本精确核查。均保持搜索片段范围。
- 内容校验器：问题 ID、自己所属的回答、连接两端对应摘录、关系理由、示范路径连续性全部通过。
- 功能：ego-browser 实走三条路径并返回。自由搜索“如何学习摄影”返回 6 个真实问题；加入图谱、分享接收、刷新后的后续进度均恢复成功。
- 阅读位置：从原位置 420px 切到另一问题后返回，恢复到 420px。修复了快速切换时滚动事件尚未送达导致的位置丢失。
- 键盘：聚焦分享按钮后按 Enter 能打开分享框，焦点进入链接输入；Escape 关闭行为已检查。
- 手机：390×844 画布与底部阅读面板检查；320px 窄屏无横向溢出；减少动态时 transition 为 0s。当前问题在画布初始化后定位，避免缩成无法阅读的全图。
- 测试：42 项通过，含问题/文章区分、答案去重、交汇、循环、收起、分享校验、无效输入、空结果、上游异常、超时、限流、配额、索引故障和关闭搜索。
- 数据库：实际事务验证 allowed / burst / quota，最后回滚，没有重置实际额度。公开问题索引的受限函数在开发、线上环境均已安装；旧 main 与新 questions-v2 独立保留。
- 类型检查、ESLint、Stylelint、生产构建与提交钩子通过。构建仍有模板大包警告，未进行真实慢速移动网络性能测量。
- 视觉：完成同内容三方向对照。结构门通过，审美状态为 candidate / needs-human-review。

## 限制

精选内容集中在自学与编程，不能据此宣称已覆盖全部知乎话题。自由探索展示的是检索关联，未经核对的语义关系不会升级成精选关系。知乎原文可能要求知乎登录；本站公开免登录与第三方原文访问条件分别处理。

真人使用效果、真人审美、实体手机、完整辅助技术兼容性尚未验证。没有联系试用者、官方、发群消息或提交赛事。

## 线上发布与匿名实测

2026-09-07 发布完成，release `7682728221788670930`，运行代码 `70ac88cf5d16cb7eece73656b427532da1540015`。发布状态为 `finished`，错误日志为空。入口、三条示范路径和恢复指引见 [发布记录](question-map-release.md)。

- 匿名 HTTP：仅带配对的 CSRF 标记，无登录凭证；首页、v2 图谱和旧图谱均为 200。线上 v2 内容逐项等于核对后的本地内容包。
- 真实搜索返回 6 个问题，重复请求命中缓存；读取新问题索引成功。上下文展开返回 5 个问题与 5 条检索关联。非法 ID 为 400，不存在 ID 为 404，空检索词为 400。
- ego-browser：在隔离环境清空 Cookie 并确认数量为 0 后进入网站，首页和搜索正常；移除设备进度后打开实际分享链接，恢复了搜索发现的问题及来路。
- 三条示范入口在线打开正确；390×844 画布、底部阅读面板实际查看，无横向溢出。无效分享安全回到起点并提示；旧 `?p=practice` 链接进入原阅读版。
- 异常路径中的超时、上游失败和配额耗尽以自动测试验证，没有故意耗尽线上用户额度。实体手机与慢网性能仍未验证。

紧凑证据在项目仓库外 `deliverables/question-map/online-api.json`、`online-browser.json` 与 `online-*.png`；不把浏览器原始日志、凭证或来源正文提交进 Git。
