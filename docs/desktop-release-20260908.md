# 知路桌面交互版 · 2026-09-08 发布

[公开入口](https://icnimtns216g.feishuapp.com/app/app_17dg665m8tq)。用户明确要求发布当前桌面版本。

- 应用：`app_17dg665m8tq`。
- 发布：`7683062359245802436`，状态 `finished`，2026-09-08 15:30:32（上海时区）。
- 运行提交：`f4cd79334d9ddc43824320196850b0cb9c345ce9`，分支 `sprint/default`。
- 公开范围仍为 `All`、`require_login=false`，未更改访问权限或内容库。
- 上一发布：`7682728221788670930`，运行提交 `70ac88cf5d16cb7eece73656b427532da1540015`。

## 线上回读

无登录身份的 HTTP 请求验证：首页 200，静态资源路径包含本次运行提交；图谱接口 200，版本 2.0.0、8 个问题、11 条连接、12 条来源。请求只有同值 CSRF cookie/header，无账号 cookie 或 Authorization。

ego-browser 1440×1000 验证：卡片 208×137；搜索入口为“搜索问题”；悬停出现原话且不改轨迹；点击打开对应问题；原文链接指向确切回答；引用讨论带入相同原话，状态明确为“讨论预览 · 尚未联网”。匿名检查、浏览器回执与截图在仓库外 `../deliverables/zhilu-release-20260908/`。

发布前已通过 48 项单元测试、lint、生产构建；提交钩子再次通过 lint。仅发布代码，未写入公开讨论、未同步本机草稿、未联系测试者。

## 功能边界与恢复

讨论仍只有本机草稿，不具备多人实时收发。手机按用户要求暂缓本轮验收。用户发布指令是本次发布依据，不推导真人使用效果已通过。

回退时在无冲突的工作分支撤销 `f4cd793`，推送并创建新发布，等待 finished 后核对线上资源及匿名图谱。保留内容库；无需删除表或恢复数据。发布后补充本文件的文档提交不改变以上运行版本。
