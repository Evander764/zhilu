# 第 6 组线上差距与最小修复方案

## 当前状态：已发布并复验

2026-09-15，发布 `7685430704193162457` / 提交 `033bac54931df283ab39c242a3b36b5849409fa2` 已 finished。已新建独立的第6组搜索密钥，只通过内存和标准输入存入本应用 online 的 `ZHIHU_ACCESS_SECRET`，并读回比对；原 Keychain 未改变。密钥仍共享所属知乎账号的试用额度，提供方权限为完整API，网站仅向外暴露固定搜索请求。

预算的13项结构变更已迁移到 online，函数 owner、固定 search_path、RLS 和限制性策略已读回。真实 related 搜索返回8问题、9回答、1文章，缓存及匿名账号隔离通过。最终页面、发布脚本和源码均未命中实际搜索密钥。并行 OAuth 改动及其回调登记门禁完整保留。

以下为准备阶段的历史核验，不能据此把当前版本误判为尚未配置。

核验边界固定为 profile `zhihu-hackathon-group6`、应用 `app_17dut1cfq4a`。线上仅查询环境键、表清单与函数是否存在，不读取变量值或个人行；开发环境仅新增下述预算结构并用可回滚夹具验证。

2026-09-14/15 读回：online 环境键清单为空；仅有 zhihu_demo_profiles、zhihu_demo_activity；`to_regprocedure('zhilu_reserve_search(integer)') IS NOT NULL` 为 false。旧 `/api/zhilu/status` 503 提示阅读资料无法加载，来自其优先读取缺失 catalog；旧 `/api/zhilu/search` 503 提示搜索未能完成，与额度函数缺失的路径一致。这不是空结果。

当前 search 的错误分支表明可执行路径检查已通过（由源码与返回分支推断）；没有云端进程/版本执行证据，不能声称云端 CLI 真搜索通过。本机官方 CLI 0.5.0 实际返回 10 条，能形成 8 问题、9 回答、1 文章，只证明本机官方搜索与归组可用。

## 2026-09-15 协调复验进展

代码候选 `adb12708c82b3e154159474c3877706fc2e76286` 已独立复跑：134测试、lint、生产构建、原文/布局/账号边界、组件内存渲染和验收器敏感性检查通过。候选尚未部署，像素与实际浏览器操作仍待在线验证。

第6组 dev 现已新增预算表与函数，以下已实测通过：匿名/登录角色不能直接读、插、改、删计数；函数正常放行、阻止两秒内重复、把请求者提交的更大上限钳制在每日200次；计数不保存调用者身份。返回值使用 `IS DISTINCT FROM` 比较并校验真实计数增量；将函数结果换成 NULL 的负测实际触发预期断言。测试回读剩余行数为0，临时测试策略及临时表均无残留。online 尚未迁移，搜索密钥来源仍待维护者确认。

平台当前拒绝直接 GRANT/REVOKE；权限由平台管理，应用使用限制性行策略。实际方案为 RLS ENABLED，受限 SECURITY DEFINER 函数按所属表的 owner 权限操作，不追加未经验证的 FORCE RLS。四个审计字段保留，但人员字段无自动采集默认值，函数显式写 NULL。

另加 `AS RESTRICTIVE` 拒绝策略，已用临时 permissive true 策略验证不能放开访客读写。函数的表引用显式限定所属 schema，`search_path` 为 `pg_catalog, 应用schema, pg_temp`；匿名/登录角色创建同名临时表后的真实调用仍使用正确计数表。以上修订已由独立任务只读复核通过。

此 CLI 现场出现 BEGIN/DO/ROLLBACK 后仍保留测试行的行为，不能仅凭 ROLLBACK 回执声称清理完成。验证脚本改为单个 DO 内的异常子事务回滚，并断言零残留；首次产生的三条测试行已按精确测试值清理。创建脚本也包装为单个 DO，避免依赖跨语句事务；该完整包装未重建当前已有表，只完成组成语句与当前 dev 状态验证。

已准备 `scripts/zhihu-demo/related-budget-migration.sql`（新建开发预算结构）和 `verify-related-budget.sql`（仅空开发库的角色/限流/零残留测试）。不要在有真实计数的线上库运行后者。

接续顺序：

1. dev 已创建单独的 zhilu_budget（id、unique bucket、used 与四个审计字段）；RLS 与受限函数权限已按上述实测收敛，勿重复创建或放宽策略。
2. 预算函数已安装：SECURITY DEFINER、固定 search_path、全局两秒频率限制、上海自然日额度、请求上限最多200。不得照搬旧脚本中的GRANT/REVOKE，不要让个人数据表承载预算，也不要恢复旧catalog来修新接口。
3. 已读取最新 dev→online 差异：13项，仅涉及预算表、函数、5个策略、约束及注释，无个人数据表变化。尚未迁移；正式迁移前再次读差异并验证目标 schema 重写，随后生成 schema。
4. 目标组织维护者通过正式密钥入口提供有授权来源的 ZHIHU_ACCESS_SECRET；保留默认 CLI transport、最多 10 条，不搬用个人 Keychain 或原组织密钥。无密钥不能放行真实搜索。
5. 同一应用候选构建时执行既有 CLI 打包流程，核对产物路径、可执行性和版本。通过正式发布后调用新 related 端点验证真实搜索及缓存；旧 status 不用作这项验收门。

不可用时继续显示明确错误并保留正文。不得绕过额度、伪造搜索结果、自动创建新应用或改账号逻辑。
